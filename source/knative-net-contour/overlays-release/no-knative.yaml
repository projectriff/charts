#@ load("@ytt:overlay", "overlay")

#! Remove resources not in the knative-serving namespace

#@ def match_nor(match_func_a, match_func_b):
#@   def match_func_wrapper(a,b,c):
#@     return not(match_func_a(a,b,c) or match_func_b(a,b,c))
#@   end
#@   return match_func_wrapper
#@ end

#@overlay/match by=match_nor(overlay.subset({ "metadata": { "namespace": "knative-serving" } }), overlay.subset({ "metadata": { "name": "knative-contour-core" } })), expects="0+"
#@overlay/remove
---
